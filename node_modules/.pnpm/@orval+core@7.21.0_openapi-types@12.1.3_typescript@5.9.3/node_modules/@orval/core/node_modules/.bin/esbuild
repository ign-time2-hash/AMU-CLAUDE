#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/Users/gabrielhenrique/Documents/AMU-CLAUDE/node_modules/.pnpm/@orval+core@7.21.0_openapi-types@12.1.3_typescript@5.9.3/node_modules/esbuild/bin/esbuild
